{
  "framework": "vite",
  "buildCommand": "cd artifacts/mockup-sandbox && pnpm run build",
  "outputDirectory": "artifacts/mockup-sandbox/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
