{
  "framework": "vite",
  "buildCommand": "pnpm install && cd artifacts/mockup-sandbox && BASE_PATH=/ pnpm run build",
  "outputDirectory": "artifacts/mockup-sandbox/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
